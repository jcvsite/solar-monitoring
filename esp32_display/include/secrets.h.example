#pragma once
// Optional compile-time WiFi fallback (portal is preferred on first boot).
// Copy to secrets.h if you want hardcoded credentials; otherwise leave placeholders
// and use the on-device SoftAP portal ("SolarDisplay-Setup").

#ifndef WIFI_SSID
#define WIFI_SSID ""
#endif

#ifndef WIFI_PASSWORD
#define WIFI_PASSWORD ""
#endif

#ifndef WIFI_SETUP_AP_NAME
#define WIFI_SETUP_AP_NAME "SolarDisplay-Setup"
#endif
